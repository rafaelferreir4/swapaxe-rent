/**
 * @file
 * swapaxe behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.swapaxe = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
