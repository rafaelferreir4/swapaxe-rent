/**
 * @file
 * swapaxe behaviors.
 */
(function (Drupal, $) {
  "use strict";

  Drupal.behaviors.swapaxe = {
    attach(context, settings) {
      // Your swapaxe JavaScript code goes here
    },
  };
})(Drupal, jQuery);
